# Copy to .env and fill in real values. .env is git-ignored.

# Base URL of the Jenkins instance. When running via docker compose, this
# defaults to http://host.docker.internal:8080 so the container can reach
# Jenkins running on your host machine.
JENKINS_URL=http://localhost:8080

JENKINS_USER=
JENKINS_API_TOKEN=
